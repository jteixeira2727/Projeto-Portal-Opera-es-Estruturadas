# Planilhas financeiras e PDF fonte (grandes, confidenciais, não editados aqui)
/*.xlsx
/*.pdf

# Screenshots/demos
/Claude outputs/

# Documentos legais/financeiros brutos de cada operação (confidenciais) —
# mas mantém os memoria.md que criamos, esses sim entram no histórico.
/Operações/*/*
!/Operações/*/memoria.md

# Pipeline: dependências e artefatos 100% derivados (template + dados já
# versionados) — regeneráveis com combine_build.py, sem motivo pra versionar
# o HTML final duplicando o que já está no template + no JSON.
/Pipeline/node_modules/
/Pipeline/portfolio_data.json
/Pipeline/portal_publish.html
/Pipeline/portal.html
/Pipeline/portal_localtest.html
/Pipeline/log.txt

# documents_data.json e portfolio_data_wrapped.json SÃO versionados
# (decisão 2026-09-17: repositório privado, e são a base de dados que o
# site vai consumir no deploy) — não adicionar aqui.

# Python
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Configuração local pessoal do Claude Code (não é do projeto)
/.claude/settings.local.json
